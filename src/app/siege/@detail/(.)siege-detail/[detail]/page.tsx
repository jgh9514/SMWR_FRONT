import MonsterDetailPage from '../../../siege-detail/[detail]/page';

export default function SiegeDetailDrawerPage() {
  // Drawer 껍데기는 parallel route(@detail) layout에서 담당합니다.
  return <MonsterDetailPage />;
}

