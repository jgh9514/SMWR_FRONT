/**
 * 검색 관련 타입 정의
 */

export interface CodeGroupSearchData {
  bsns_cd?: string;
  dtl_bsns_cd?: string;
  cd_grp_no?: string;
  cd_grp_nm?: string;
}

export interface CodeSearchData {
  cd_grp_no?: string;
  cd?: string;
  cd_nm?: string;
}

